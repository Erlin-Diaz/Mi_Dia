{
  "name": "mi-dia",
  "version": "1.0.0",
  "private": true,
  "description": "Organizador diario (app estática, sin build).",
  "type": "module",
  "scripts": {
    "test": "node --test tests/logic.test.mjs"
  }
}
